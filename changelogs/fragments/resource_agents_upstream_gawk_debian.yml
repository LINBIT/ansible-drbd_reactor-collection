# SPDX-License-Identifier: MIT
---
bugfixes:
  - >-
    resource_agents_upstream - install ``gawk`` on the Debian OS family. Without
    it, ``IPaddr2`` failed to start because ``findif.sh`` needs gawk extensions
    that ``mawk`` lacks.
