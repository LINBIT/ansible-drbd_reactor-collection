# SPDX-License-Identifier: MIT
---
bugfixes:
  - reactor_install - ship the drbd-reactor-reload path and service units
    with the role instead of copying them from the package documentation
    directory. Minimal images and nodocs installs strip that directory, so
    enabling automatic reload failed with "Source not found" even though
    drbd-reactor itself installed fine.
