---
skip_list:
  # Both roles use underscore-prefixed private variables (_ra_*, _reactor_install_*)
  # scoped to a single role. The leading underscore already signals "internal";
  # enforcing the full role name prefix adds verbosity without safety benefit.
  - var-naming[no-role-prefix]
