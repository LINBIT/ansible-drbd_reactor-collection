# SPDX-License-Identifier: MIT
---
- name: Open UFW ports for the DRBD Reactor Prometheus exporter
  community.general.ufw:
    rule: allow
    port: "{{ item.port | regex_replace('-', ':') }}"
    proto: "{{ item.proto }}"
  loop: "{{ reactor_install_firewall_ports }}"
